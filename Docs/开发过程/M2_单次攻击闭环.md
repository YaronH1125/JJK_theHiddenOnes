# M2：单次攻击闭环

版本：v0.4；更新日期：2026-09-16；实施与自测状态：输入资产缺失已修复，冷启动自动验收 57 项通过；T09 帧率矩阵沿用此前证据。证据与记录见文末验收记录。

[开发过程导航](README.md) · 上一阶段：[M1](M1_基础训练擂台.md) · 下一阶段：[M3](M3_连招与核心攻防.md)

## 0. 开发 agent 阅读顺序与范围

1. [开发约束](../03_开发约束与决策记录.md)、[需求说明](../01_需求说明.md)：确认已定范围、GAS 约束和可调整建议。
2. [架构设计](../02_架构设计.md)：重点读输入请求、GA/GE 职责、近战检测、结算与清理。
3. [开发总计划](../05_开发计划与验收.md)、[M1](M1_基础训练擂台.md) 的实际验收记录：确认现有基座、已完成项和限制；不要重建 M0/M1。
4. 本文全文与[阶段记录模板](README.md)：以本阶段交付、自测和闭环为执行清单。
5. [石流龙战斗系统设计](../08_石流龙战斗系统设计.md)：重点读第 4、5、9、10、12 节，理解左键拳击/长按重拳意图、动作边界与后续接口。完整角色规则以 08 最新正文为准；本阶段只实现下述子集。
6. 按需读[角色方案](../06_石流龙角色方案.md)、[M1 素材检查](验收记录/M1_石流龙素材检查.md)；正式石流龙模型不是 M2 前置条件，沿用 Quinn 与模板拳击占位。旧工程不是本阶段默认阅读或复制来源。

随后核对当前工程相关输入、角色/ASC、属性、GameMode、重置和资产的实际实现，不把文档中的建议类名当作已存在代码。若仓库/父目录存在适用的 AGENTS.md，先遵守其开发约束。

M2 只做近战第一段拳击与轻重输入识别小原型，不做完整重拳伤害/动画、腿击、连招、Shift 闪避取消、F 防御、E 切形态、远程瞄准/炮击或领域。这些分别在 M3/M6 落地；领域消耗与熔断的讨论不阻塞本阶段。

## 1. 目标与进入条件

用一次普攻打通输入、GA、动画、检测、GE、受击、恢复和重置。M1 的双方角色、独立 ASC、目标与白盒必须可运行；准备一段可用攻击 Montage 和受击占位动作。

本阶段只实现一个攻击段，不提前做三段连招、完整防御、正式 UI 或炮击。必须提前做最小死亡禁止动作与异常清理，不能把延迟伤害问题全部推迟到 M3。

## 2. 交付与职责

| 交付项 | 职责 |
| --- | --- |
| 共享动作请求入口 | 玩家和开发调试对手都提交攻击意图，返回执行或拒绝原因 |
| MeleeCombo 的单段原型 | GA 管激活、Montage、窗口、结束与取消 |
| AttackDefinition 单段配置 | 动画、命中段、伤害、轨迹参数与攻击类型 |
| CombatHitComponent | 活动窗口采样、攻击实例记录、命中去重与结算入口 |
| 伤害 GE 与 HitReact | 正常伤害修改属性，产生受击/中断与恢复 |
| 调试重置入口 | 集中取消战斗、恢复属性与位置，供后续面板复用 |

动画通知只报告事件，不直接扣血或持有跨角色状态。Cue/粒子只做表现。具体对象分工见 [架构设计](../02_架构设计.md)。

## 3. 开发步骤与局部检查

### M2.1 先验证请求到能力

将攻击 Input Action 映射到项目请求，检查角色存活、ASC 初始化和当前动作限制，再尝试激活已授予能力。能力不能在每次按键或重置时重新授予。

先让 GA 只打印开始、结束和拒绝原因，确认玩家与调试对手走相同入口。重复按键不产生同一角色的重复活动实例。

局部检查：一次合法输入对应一次激活；死亡/未初始化时拒绝，失败有原因。

追加最小轻重输入原型：左键按下建立 InputSessionId，阈值前松开才提交单段轻拳；达到建议 0.18 秒阈值则记录 HeavyPunch 意图，暂以无伤害占位/调试结果验证，不再补发轻拳。松开、被中断、死亡、菜单/失焦或重置清会话；旧松键不能补攻击。阈值为起测值，重拳的实际动作与伤害交 M3。本阶段不必实现完整单槽连招缓存。

### M2.2 接动画和窗口生命周期

GA 播放 Montage，管理起手、有效、恢复三个阶段。通过通知打开/关闭检测，事件带当前动作实例身份，拒绝旧动画遗留事件。

所有退出路径主动关窗口、停止相关任务并清理临时状态；不能等最后一个通知来恢复角色。通知丢失或 Montage 播放失败也要结束能力。

局部检查：正常结束和强制停止 Montage 后均能再次攻击，检测不残留。

### M2.3 做轨迹检测与跨采样去重

在有效窗口按拳部 Socket 前后位置 Sweep，过滤自身和非法目标。去重键为攻击实例、命中段与目标，集合保持到该段结束；新攻击可再次命中。

快速位移/旋转按轨迹跨度补采样，先在调试显示中确认轨迹，再连接伤害。不把“每帧有一次命中”当作合理的多段伤害。

局部检查：同一目标多碰撞部件、同一窗口多次采样都只产生一条有效命中。

### M2.4 接统一结果与受击

结算先检查有效性、去重和无敌等已具备规则，再生成伤害结果，经 GE 修改生命、通知受击和统计。未实现的防御/投技留清晰扩展点，不写伪成功结果。

HitReact 明确打断哪些活动能力，恢复后允许行动；最小死亡处理取消活动攻击、禁止新动作并等待调试重置。受击和死亡均不能只改动画而不改规则状态。

局部检查：一次命中只扣一次血、产生一次受击；受击结束后可再次请求动作。

### M2.5 明确同批次命中顺序

为测试场景定义“同批次”为本次命中收集后统一处理的一批接触结果。默认先筛出有效普通攻击接触，再按稳定次序结算，允许这一批已有效接触换血；被打断后未来窗口产生的接触不再有效。

记录批次标识与双方结果。若现有实现直接即时结算导致遍历顺序决定胜负，应修正本批处理方式，不把它称为已支持换血。致死同批次结果也需记录，不要求实现联网确定性。

局部检查：固定双方位置与出手时刻重复触发，结果符合记录规则。

### M2.6 接入最小调试与重置

显示动作请求结果、当前 GA/阶段、关键标签、攻击实例/命中段、命中次数、双方生命。重置顺序沿用架构文档：停请求 → 取消能力 → 清临时对象/事件 → 复位双方 → 恢复属性和统计 → 恢复目标与模式。

开发调试入口可触发对手攻击、强制中断和重置；动作仍经过共享请求，直接伤害仅用于明确标记的异常注入。M4 再接正式训练面板。

局部检查：在起手、有效、恢复各阶段重置，等待超过原动作时长后没有补发命中。

## 4. 本阶段自测

| ID | 前置与操作 | 预期结果/观察点 |
| --- | --- | --- |
| M2-T01 | 有效距离攻击一次，再在范围外攻击 | 前者一次 GE/受击；后者空挥，无远程扣血 |
| M2-T02 | 同目标多碰撞部件、有效窗口连续查询 | 同一实例同一段仅一次伤害；记录去重键 |
| M2-T03 | 前一次攻击结束，再攻击同目标 | 新实例可再次命中，旧去重记录不阻止新攻击 |
| M2-T04 | 快速连续按攻击；在禁止状态提交请求 | 无重复激活，拒绝原因可见 |
| M2-T05 | 起手/有效/恢复时分别停止 Montage 或注入受击 | 窗口与状态清理；恢复后可再次出招 |
| M2-T06 | 在上述阶段分别死亡/重置，等待超过原动作时长 | 无延迟伤害、重复授予或残留状态；死亡时不能出招 |
| M2-T07 | 攻击过程中销毁目标或结束 PIE | 无失效引用访问、残留回调错误 |
| M2-T08 | 双方调试同步攻击，至少重复 5 次 | 同批次有效普通接触按记录换血；中断后未来接触失效 |
| M2-T09 | 30/60/120 FPS 下复现一次快速攻击 | 无明显漏判或重复扣血，记录实际帧率与轨迹差异 |
| M2-T10 | 仅关闭命中粒子/声音后重复 T01 | 伤害和受击规则保持，表现不控制结算 |
| M2-T11 | 左键在轻重阈值前/后松开、长按后重复松开 | 短按执行一段轻拳；长按只记录重拳占位意图，无轻重双发、重复提交或重拳伪伤害 |
| M2-T12 | 按住左键后失焦/菜单/死亡/重置，再恢复并松开 | 旧会话失效，不补拳；新按下后可重新完成轻拳闭环 |

当前均未执行。M2-T05/T06 可用开发入口触发，不要求此时已有完整攻防菜单。

## 5. 必须演示的闭环

进入擂台 → 攻击命中一次 → 查看生命与受击 → 对手恢复 → 再攻击 → 动作中重置 → 等待旧动作时长后确认无伤害 → 再次攻击正常。另演示一次同批次换血。

## 6. 完成标准与交接

- [x] 输入到 GE/受击的完整链路有日志或录像证据。
- [x] 同段去重、正常结束、中断、死亡、重置和目标失效通过自测。
- [x] 同批次换血规则、测试方式和处理顺序已记录。
- [x] M1 的生成、自由镜头和独立属性做相关回归。
- [x] 向 M3 交接窗口事件、请求结果、攻击定义、命中结果和重置入口。
- [x] 轻重输入识别与会话清理有证据，明确重拳只是占位意图，实际动作/伤害待 M3。

重复伤害、残留窗口、重置后补发攻击或永久无法行动均阻塞 M3。动画精细度和正式 HUD 不阻塞。

## 7. 排查与记录

重复伤害先看去重集合生命周期与多碰撞命中；命中无受击先查 GE/事件链路；动画结束仍不能动先查 GA 结束和标签来源；偶发旧命中先查实例校验与延迟回调。

### 阶段验收记录：M2 / 2026-09-16

- 实现版本或快照：Git `c4ac471`（M1 收尾）之上开发 M2；含两次构建修复与一次换血确定性重构，记录见 [构建日志](验收记录/M2_Build_1.log)。
- 环境、地图、构建与运行方式：UE 5.8.2-56702186，Win64 Development Editor，编辑器关闭后 UBT 构建；地图 `L_TrainingArena`，GameMode `BP_TrainingGameMode`；PIE 由 runner 管理（UE-MCP），断言脚本经 Python Remote Execution 在编辑器内执行，逐项写 [M2_Acceptance.json](验收记录/M2_Acceptance.json)。
- 本次修改及原因：
  1. 新增 `UCombatInputComponent`（共享请求入口 + 轻重输入会话）、`UMeleeComboAbility`（单段 GA）、`UAttackDefinition`（DA_M2_Attack_A1）、`UCombatHitComponent`（窗口扫掠/去重/批次结算）、`UDamageGameplayEffect`（SetByCaller 伤害 GE）；FighterCharacter 挂载组件并扩展死亡/受击/重置；ArenaPlayerController 绑定左键与失焦清理；TrainingGameMode 增加重置序列与调试命令。
  2. 换血确定性重构（M2.5）：命中事件由受击方在其命中组件 Tick 内、扫掠之后处理，保证同帧互中双方已完成的接触都有效换血。
  3. 修复：MeleeAttackAbility 未授予导致激活失败（失败原因误报 AlreadyActive，已区分）；5.8 API 改名适配（LooseGameplayTag、AnimNotify 签名、Slate 激活事件）；补 GameFeatureData 资产登记消除启动告警。
- 关键配置/占位资产：`DA_M2_Attack_A1`（35 伤害、段 0、hand_r、半径 20、硬直 0.5s）；`AM_M2_A1`（MM_Attack_01 程序化构建）、`AM_M2_HitReact`（MM_HitReact_Front_Lgt_01）；`IA_Attack`（左键）入 `IMC_Training`；角色定义 `DA_Fighter_Ishigori` 关联以上资产。

| 用例 ID | 实际结果 | 状态 | 证据位置/问题编号 |
| --- | --- | --- | --- |
| M2-T01 | P1 位于 X=400、P2 位于 X=500（相距 100cm）攻击恰好 -35 且硬直期请求 REJECTED_BLOCKED；相距 1000cm 空挥无伤害 | 通过 | [M2_Acceptance.json](验收记录/M2_Acceptance.json) T01_* |
| M2-T02 | 窗口期连续多帧接触（等效多部件/多帧覆盖）去重后仅 1 次伤害 | 通过 | 同上 T02_dedup_single |
| M2-T03 | 新攻击实例 id 递增且可再次命中（累计 -70） | 通过 | 同上 T03_new_instance_hits |
| M2-T04 | 连按 5 次仅 1 次 EXECUTED，其余 REJECTED_ALREADY_ACTIVE；硬直期 REJECTED_BLOCKED | 通过 | 同上 T04_* |
| M2-T05 | 起手/有效/恢复三阶段注入受击：窗口与攻击状态清理、可再出招 | 通过 | 同上 T05_* |
| M2-T06 | 三阶段死亡均无延迟伤害、死后 REJECTED_DEAD、重置复活且初始化次数仍 1；三阶段重置无补发 | 通过 | 同上 T06_* |
| M2-T07 | 攻击中销毁目标无失效引用；重生（Fighter_P2_0 唯一名）后目标重分配、可再命中 | 通过 | 同上 T07_* |
| M2-T08 | 双方同帧互攻 ×5，每轮各 -35（同批次已有效接触换血） | 通过 | 同上 T08_trade_1…5 |
| M2-T09 | 30/60/120 FPS（实测 30/60/99）各一次攻击，均恰好 1 命中 35 伤害 | 通过 | [M2_FPS.json](验收记录/M2_FPS.json) |
| M2-T10 | 命中调试表现开/关（JJK.DebugHitFX）伤害链路一致 | 通过 | 同上 T10_fx_toggle_same_damage |
| M2-T11 | 短按触发轻拳一次；长按仅记录 HeavyPunch 意图（无动作/伤害）；无会话松开被拒 | 通过 | 同上 T11_* |
| M2-T12 | 按住后重置/失焦/死亡三路径会话失效、旧松键不补拳；重新按下完成轻拳闭环 | 通过 | 同上 T12_* |
| M2-T13 | 冷启动读取正式输入资产与左键映射；Enhanced Input 按下/松开经过 Controller，触发攻击/受击 Montage、单次 -35；长按无轻拳 | 通过 | 同上 T13_*（9 项） |

场景截图：[M2_闭环演示.png](验收记录/M2_闭环演示.png)（仅证明蓝 P1 / 橙 P2 近身对峙；画面无法辨识调试 HUD 与调试球，伤害及状态结论以自动验收证据为准）。

- 完整闭环演示结果：进入擂台 → 命中一次（-35，硬直，硬直内请求被拒）→ 硬直恢复 → 再攻击 → 动作中重置（等待超动作时长无补发）→ 再次攻击正常 → 同批次换血 ×5，均经自动化断言。
- 失败复现与修复后复测：
  1. 首次运行 T01 失败：MeleeAttackAbility 只配置未授予（TryActivate 失败且失败原因误报）——修复授予链路与原因区分后复测通过。
  2. 换血不确定（受击方 Tick 先于扫掠处理中断）——事件处理移入命中组件 Tick 内扫掠之后，5 轮互攻对称换血。
  3. 原输入修复只在内存中建立 IA_Attack，正式文件缺失，原“真实分发验证通过”声称缺少证据。现以 `save_loaded_asset` 保存到 Content，断言文件存在且 Git 可跟踪，编译保存 Controller；重启编辑器后 T13 通过，证明 Enhanced Input action 分发链路有效。
  4. 验收脚本原先在迭代结束时无条件标记 passed，已改为检查所有断言；外部 runner 再检查 57 项完整性和逐项状态，并记录输入资产 SHA-256。
  5. 移除 M0 GAS 链接探针；[M2_Build_2.log](验收记录/M2_Build_2.log) 构建成功，[DLL 导入表](验收记录/M2_GAS_Imports.txt) 确认三个 GAS 模块仍被实际引用。
- 未验证项/已知限制/后续阶段：
  - 命中窗口为配置时间窗口（DA 开关 `bWindowFromAnimNotifies=false`）；通知类已实现，Python 无法写 Montage 受保护 Notifies 字段，通知路径待资产编辑器手工添加通知后启用（偏差已记录）。
  - 无正式 Cue/粒子/声音（结算不依赖表现，T10 以表现开关验证）；正式表现留 M6。
  - 实体键鼠人工体验、长时手感、独立打包未做（不阻塞 M3）。
- 进入下一阶段：是；依据：冷启动 T01—T13 共 57 项通过；输入为 Enhanced Input action 注入并核对左键映射，未进行操作系统鼠标模拟。M1 独立回归结果见修复复验记录。
- 下一步：进入 M3 连招与核心攻防（三段连招缓存、轻重击完整实现、防御/闪避/取消、受击死亡完整串联）。

