# M5 实施记录

## 第一阶段：原生行为树与核心接入（2026-09-16）

- 移除旧 Tick/Timer 决策框架，使用 UBehaviorTree + UBlackboardData/Component；原生树随模块构建，Root Sequence → 决策 Task → 七分支 Selector。
- 动作 Task 在提交共享请求前订阅 ASC 激活/结束委托，跟踪单槽缓存编号，退出只清自身订阅与缓存。GameMode 统一停止/恢复 AI、菜单请求门禁及胜负重开。
- 导航使用 SupportedAgents(Arena)：半径 42、高 192 cm，与角色 Capsule 42/96 一致；动态生成，固定 1024 瓦片池。直接修改 Recast 代理尺寸会在冷启动被项目默认值覆盖，已改为项目配置。
- Development Editor 构建成功；真实图形 PIE 核心 30 项检查全部通过，含八方向场内路径、场外拒绝、接近、真实伤害、菜单停树、三种胜负各一次与三次重开。证据：M5_Build_Core.log、M5_Core_30.json。
- 当前为阶段性检查，缓存/瞬时结束/资源冷却/移动争抢等边界、M3/M4 回归与独立包仍待后续完成，尚不标记 M5 完成。

## 第二阶段：动作边界与战斗控制权（2026-09-16）

- 原生树调试分支复现入口只在非 Shipping 有效；真实 Task 同样进行规则、缓存、ASC 实例结束和委托清理。
- 寻路中止使用 Keep velocity，保留已发生击退/闪避；正常距离分支间保留有效路径，避免周期性刹停。闪避成本读取角色定义和 ASC 无限资源规则。
- 结果自动打开 UMG 面板，三种模式进入统一下拉框；保留独立 AI 快捷按钮与当前分支/任务状态。
- 修复 CompleteRestart 在本机引擎中的停止行为，开发入口改为显式 StopTree/StartTree；正式模式启动路径不受影响。
- 扩展 PIE 68 项全部通过（nullrhi，真实世界、动画、碰撞、GAS 和 BT；不作为视觉验收）：缓存消费/过期/覆盖/中止，瞬时 Probe 能力结束与真实 GE 冷却，资源不足，受击拒绝，目标销毁与重生，输入未表现时不反制，反应延迟对比，三种距离行为，边界回退，真实重拳击退/重踢倒地/条件投技，同招伤害 35，胜负仅一次及三轮重开。
- 击退专项临时将对手 MaxWalkSpeed 设为 10 cm/s 并禁防御/闪避以保证受击前置，仍保持真实 AI 寻路；测量正向击退峰值并断言路径 Idle。之后恢复速度，完整回归使用默认配置。
- 独立包专用 -M5SmokeTest 验收入口已构建，普通游戏不创建；包内运行结果留待下一阶段归档。
